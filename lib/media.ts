// helper utilities (placeholder for future processing)
import fs from "fs";

export async function processMedia(filePath: string) {
  // implement resizing, conversion, cleanup if needed
  // No momento, apenas retorna o path.
  return filePath;
}
